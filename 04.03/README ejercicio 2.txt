Se ha modificado el documento para que no haya conflictos en el documento XML.

- He utilizado espacios de nombres:
  - xhtml: Para los elementos de HTML (estructura visual).
  - book: Para los datos del libro (título, autor, precio, páginas, fecha).
